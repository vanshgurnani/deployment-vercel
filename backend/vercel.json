{
  "version": 2,
  "builds": [
    {
      "src": "index.js",  // Entry point for the application
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",  // Proxy API requests
      "dest": "index.js"
    }
  ]
}
